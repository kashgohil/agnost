// Tailwind v4 uses a single PostCSS plugin. No tailwind.config.ts required —
// theme tokens live in CSS via @theme in globals.css.
const config = {
  plugins: ["@tailwindcss/postcss"],
};
export default config;
